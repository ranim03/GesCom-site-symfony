<form method="post" action="{{ path('app_detailfacture_delete', {'id': detailfacture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detailfacture.id) }}">
    <button class="btn">Delete</button>
</form>
